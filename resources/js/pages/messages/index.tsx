import React from 'react';
import AppLayout from '@/layouts/app-layout';

const Index = () => {
    return (
        <AppLayout>
            <section>

            </section>
        </AppLayout>
    );
};

export default Index;
